{
  "identifier": "default",
  "description": "Default capability set for Cortex Speech App",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
